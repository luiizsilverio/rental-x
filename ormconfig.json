{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "docker",
  "password": "ignite",
  "database": "rentx",
  "entities": [
      "./src/model/*.ts"
  ],
  "migrations": [
      "./src/database/migrations/*.ts"
  ],
	"migrationsRun": true,
  "cli": {
      "migrationsDir": "./src/database/migrations"
  }
}